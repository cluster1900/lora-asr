# Colab provides CUDA-enabled torch. Keep it instead of downloading another wheel.
qwen-asr @ git+https://github.com/QwenLM/Qwen3-ASR.git@7c6daf77a2421100f5fb066495372c00129d39ff
transformers==4.57.6
accelerate==1.12.0
peft==0.19.1
datasets==5.0.0
PyYAML==6.0.3
librosa>=0.11,<0.12
soundfile>=0.13,<0.14

# Install separately after the lines above so pip can reuse Colab torch:
# pip install flash-attn==2.8.3.post1 --no-build-isolation
